import React from "react";
import ReactDOM from "react-dom";
import LoginForm from "./features/LoginForm.tsx/LoginForm";

ReactDOM.render(
  <React.StrictMode>
    <LoginForm />
  </React.StrictMode>,
  document.getElementById("root")
);
